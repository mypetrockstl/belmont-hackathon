USE DATABASE BELMONT_HACKATHON;
USE SCHEMA PUBLIC;

CREATE OR REPLACE TABLE MUSIC_FEATURES (
filename TEXT
,tempo DECIMAL
,beats DECIMAL
,chroma_stft  DECIMAL
,rmse  DECIMAL
,spectral_centroid  DECIMAL
,spectral_bandwidth DECIMAL
,rolloff DECIMAL
,zero_crossing_rate DECIMAL
,mfcc1 DECIMAL
,mfcc2 DECIMAL
,mfcc3 DECIMAL
,mfcc4 DECIMAL
,mfcc5 DECIMAL
,mfcc6 DECIMAL
,mfcc7 DECIMAL
,mfcc8 DECIMAL
,mfcc9 DECIMAL
,mfcc10 DECIMAL
,mfcc11 DECIMAL
,mfcc12 DECIMAL
,mfcc13 DECIMAL
,mfcc14 DECIMAL
,mfcc15 DECIMAL
,mfcc16 DECIMAL
,mfcc17 DECIMAL
,mfcc18 DECIMAL
,mfcc19 DECIMAL
,mfcc20 DECIMAL
,label TEXT
);

CREATE OR REPLACE FILE FORMAT my_csv_format
TYPE = CSV
FIELD_DELIMITER = ','
SKIP_HEADER = 1
NULL_IF = ('\\N', 'null', '')  -- Common options for handling nulls
EMPTY_FIELD_AS_NULL = TRUE
;

-- Load data from the specific file in the stage
COPY INTO MUSIC_FEATURES
FROM @MUSIC_FEATURES/data.csv
FILE_FORMAT = (FORMAT_NAME = 'my_csv_format')
ON_ERROR = 'CONTINUE'  -- Option to continue loading even if a few records fail
;
